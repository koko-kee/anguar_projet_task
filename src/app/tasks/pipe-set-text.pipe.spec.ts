import { PipeSetTextPipe } from './pipe-set-text.pipe';

describe('PipeSetTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeSetTextPipe();
    expect(pipe).toBeTruthy();
  });
});
