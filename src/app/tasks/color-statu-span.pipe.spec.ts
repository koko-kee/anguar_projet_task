import { ColorStatuSpanPipe } from './color-statu-span.pipe';

describe('ColorStatuSpanPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorStatuSpanPipe();
    expect(pipe).toBeTruthy();
  });
});
