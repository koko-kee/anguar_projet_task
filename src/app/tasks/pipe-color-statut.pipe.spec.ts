import { PipeColorStatutPipe } from './pipe-color-statut.pipe';

describe('PipeColorStatutPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeColorStatutPipe();
    expect(pipe).toBeTruthy();
  });
});
